query GetFile($id: ID!){
    node(id: $id){
        id
        ... on File {
            contentType
            name
            size
            createdAt
            updatedAt
            url
        }
    }
}

